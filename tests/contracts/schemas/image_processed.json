{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://github.com/shabohin/photo-tags/schemas/image_processed.json",
  "title": "ImageProcessed",
  "description": "Message schema for image_processed queue",
  "type": "object",
  "required": [
    "timestamp",
    "trace_id",
    "group_id",
    "telegram_username",
    "original_filename",
    "processed_path",
    "status",
    "telegram_id"
  ],
  "properties": {
    "timestamp": {
      "type": "string",
      "format": "date-time",
      "description": "Message timestamp in RFC3339 format"
    },
    "trace_id": {
      "type": "string",
      "description": "Unique trace ID for tracking the message across services",
      "minLength": 1,
      "maxLength": 100
    },
    "group_id": {
      "type": "string",
      "description": "Telegram group ID",
      "minLength": 1,
      "maxLength": 100
    },
    "telegram_username": {
      "type": "string",
      "description": "Telegram username",
      "minLength": 1,
      "maxLength": 100
    },
    "original_filename": {
      "type": "string",
      "description": "Original filename of the uploaded image",
      "minLength": 1,
      "maxLength": 500
    },
    "processed_path": {
      "type": "string",
      "description": "Storage path of the processed image",
      "minLength": 0,
      "maxLength": 1000
    },
    "status": {
      "type": "string",
      "description": "Processing status",
      "enum": ["completed", "failed"]
    },
    "error": {
      "type": "string",
      "description": "Error message if processing failed",
      "maxLength": 2000
    },
    "telegram_id": {
      "type": "integer",
      "description": "Telegram user ID",
      "minimum": 1
    }
  },
  "additionalProperties": false
}
